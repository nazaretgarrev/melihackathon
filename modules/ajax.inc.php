<?php

	$page_headers = "Content-Type: application/json";
	$use_layout   = false;
	
	echo "AJAX";

	/*
	die($security->get_token());

	print_r($meli->get('/users/me', array('access_token' => $security->get_token())));
	*/
	
?>